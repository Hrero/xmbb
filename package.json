{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run tsc && lerna publish from-git"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
